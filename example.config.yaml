# Copy this file to config.yaml and customize it.
# For public/private Runner use, keep actual secret values in environment variables or GitHub Actions secrets.

IMDB:
  AUTH: cookies
  EMAIL:
  PASSWORD:
  COOKIEATMAIN:
  LISTS: []
  IGNOREDLISTS: []
  TRACE: false
  HEADLESS: true
  BROWSERPATH:

TRAKT:
  ENABLED: true
  CLIENTID:
  CLIENTSECRET:
  TOKENFILE: trakt-token.json
  SYNC:
    MODE: add-only
    HISTORY: true
    RATINGS: true
    WATCHLIST: true
    LISTS: true
    TIMEOUT: 30m

TMDB:
  ENABLED: true
  READACCESSTOKEN:
  SESSIONID:
  SYNC:
    MODE: add-only
    RATINGS: true
    WATCHLIST: true
    TIMEOUT: 30m
